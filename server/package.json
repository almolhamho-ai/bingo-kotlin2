{
  "name": "bingo-relay-server",
  "version": "1.0.0",
  "description": "WebSocket relay + matchmaking server for the Bingo Android app (real-internet play: quick match, random tournaments, invite-by-ID, presence counts).",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "ws": "^8.18.0"
  }
}
